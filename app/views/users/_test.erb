<% current_user.confirmed_friends.each do |f|  %>
  <p><%#= f.id %></p>
  <p><%#= user.id %></p>
<% end %>

<%= form_for(current_user.friendships.build, url: friendships_path) do |f| %>
  <%= f.hidden_field :recipient_id, value: user.id %>
  <%= f.submit "Send Friend Request" %>
<% end %>
