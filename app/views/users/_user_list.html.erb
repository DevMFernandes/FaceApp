<% users.each do |user| %>
  <div class="user">
  <%= gravatar_for user, size = 60 %>
  <p><%= link_to user.name, user %></p>
  <%= render locals[:name], user: user %>
  </div>
<% end %>
<%= will_paginate users %>
